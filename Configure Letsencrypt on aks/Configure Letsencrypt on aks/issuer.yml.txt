apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-capi-stg # => name of the pod for clusterissuer, we should use this name in ingress tls secret and cert-manager annotation.
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory # => this is the acme server for prod from where certificate will be authenticated.
#https://acme-staging-v02.api.letsencrypt.org/directory for stg
    email: ammad.khalid@afia.ae
    privateKeySecretRef:
      name: account-key-prod => secret for server to authenticate, it is different for both prod and stg and by default.
    solvers:
      - http01:
          ingress:
            class: nginx